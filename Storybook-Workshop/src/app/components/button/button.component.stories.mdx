import { Meta, Story, Canvas, ArgsTable, Props, stories } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { ButtonComponent } from './button.component';
import { ButtonModule } from "primeng/button";

<Meta title='Components/ExamplePage'
      component={ButtonComponent}
      decorators={[
        moduleMetadata({
          imports: [ButtonModule]
        })
      ]}
      parameters={{backgrounds: {
                       values: [
                         {name: 'red', value: '#f00'},
                         {name: 'green', value: '#0f0'},
                       ],
                     }}}
      argTypes = {{
        backgroundColor: {control: 'color'},
        label: {
          name: 'Label style',
          description: 'lorem ipsum sit amet',
          options: ['Normal', 'Bold', 'Italic'],
          table: {
            defaultValue: {
             summary: 'Italic',
             detail: 'Text displayed by the Badge'
            }
          },
          control: {
            type: 'select'
          }
        }
      }}
/>

# Button
<p> Description </p>

## ArgsTable
<ArgsTable of={ButtonComponent} />

<!-- ## Props -->
<!-- <Props of={ButtonComponent} /> -->

### Story with Template binding

export const Template = (args) => <app-button {...args } />

<Canvas>
  <Story name="Bold" args={{
    label: 'Bold',
    type: 'secondary'
  }}
  parameters={{backgrounds: {
                         values: [
                           {name: 'red', value: '#f00'},
                           {name: 'green', value: '#0f0'},
                         ],
                       }}}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Story id='components-button--secondary-disabled'/>
